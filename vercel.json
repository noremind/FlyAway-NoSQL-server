{
	"version": 2,
	"outputDirectory": "build",
	"public": true,
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "server.js"
		},
		{
			"src": "/storage/(.*)",
			"dest": "/public/images/avatar/$1"
		}
	]
}
