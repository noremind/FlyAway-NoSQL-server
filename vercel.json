{
	"version": 2,
	"public": true,
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/storage/(.*)",
			"dest": "/images/avatar/$1"
		},
		{
			"src": "/storage/(.*)",
			"dest": "/images/avatar/$1"
		},
		{
			"src": "/(.*)",
			"dest": "server.js"
		}
	]
}
